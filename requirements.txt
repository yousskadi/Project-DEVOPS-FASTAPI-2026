## A vérifier

fastapi
uvicorn[standard]

# PostgreSQL driver (sans ORM)
psycopg[binary]

# Environment variables
python-dotenv

# Validation / settings
pydantic

# Tests (optionnel mais recommandé)
pytest
httpx

## ORM
sqlalchemy>=2.0
alembic>=2.0   # pour les migrations